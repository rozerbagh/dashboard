import React from 'react'
import { Typography } from '@material-ui/core'
export default function CustomersList(props) {
    return <span style={{ fontSize: "0.7rem", fontWeight: 400, color: "#333333" }}>{props.name}</span>
}
